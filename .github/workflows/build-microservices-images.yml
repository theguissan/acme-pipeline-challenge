name: Build Microservice Images

on:
  workflow_call:

jobs:
  build-images:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      - name: Build Docker Image Microserviço 1
        run: docker build -t microservice1:latest ./microservice1
      - name: Build Docker Image Microserviço 2
        run: docker build -t microservice2:latest ./microservice2