name: Pipeline CI/CD ACME

on:
  push:
    branches:
      - develop
      - release
      - main

jobs:

  tests:
    uses: ./.github/workflows/test.yml
    secrets:  
      SONAR_TOKEN: ${{secrets.SONAR_TOKEN}}